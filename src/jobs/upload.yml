description: Upload a file to Dropbox

executor: default

parameters:
  filepath:
    type: string
    description: "Path of file to upload"
  uploadpath:
    type: string
    description: "Where to upload file"

steps:
  - upload:
      filepath: << parameters.filepath >>
      uploadpath: << parameters.uploadpath >>
