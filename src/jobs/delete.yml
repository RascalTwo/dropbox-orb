description: >
  Delete a file

executor: default

parameters:
  deletepath:
    type: string
    description: "Path of file to delete"
steps:
  - delete:
      deletepath: << parameters.deletepath >>
