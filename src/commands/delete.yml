description: >
  Delete a file

parameters:
  deletepath:
    type: string
    description: "Path of file to delete"
steps:
  - run:
      environment:
        DELETE_PATH: <<parameters.filepath>>
      name: Delete File
      command: <<include(scripts/delete.sh)>>
